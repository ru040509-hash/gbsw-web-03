let a = 10;

// 매개변수
function add(n, m = 0) {
   return n + m; //호출부에 값을 돌려준다
} // 함수 표현식으로 만드려면 let add = 을 앞에 넣고 맨 뒤에 ; 을 넣는다

// 함수표현식
let add = function (a,b) {
    return a + b; 
};

let = obj = {};
let = arr = [];
